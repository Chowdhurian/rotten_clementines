<h1>Rotten Clementines</h1>
<%= paginate @users %>
<% @users.each do |user| %>
  <h4>email: <%= user.email %> (<%= link_to "edit", edit_admin_user_path(user) %>, <%= link_to "delete", admin_user_path(user), method: :delete, data: {confirm: "Are you sure you'd like to delete this?"} %>)</h4>
<% end %>
<hr>
<%= paginate @users %>
